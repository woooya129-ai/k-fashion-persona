# update-PLAN: v0.7.0 신뢰도 회수 업데이트

> 상태: 원안/아카이브 문서. 현재 실행 기준은 `PLAN.md`이며, 이 문서의 `0.6.2 → 0.7.0` 표기는 원안 작성 시점 기준이다.

## 현재 버전

- 현재 버전: `0.6.2`
- 다음 권장 버전: `0.7.0`
- 판단 근거: 단순 버그픽스가 아니라 제품 포지션, 결과 신뢰 방어선, 입력 구조, 리포트 구조가 같이 바뀌는 업데이트다. `0.6.3`보다 `0.7.0`이 맞다.

## PM 목표

이번 업데이트의 목표는 기능을 많이 붙이는 게 아니다. 제품 정의를 좁히고, 사용자가 결과를 “정답”이 아니라 “검증해야 할 리스크 질문”으로 읽게 만드는 것이다.

새 제품 정의:

> 실제 설문 전에, 합성 페르소나로 제품 컨셉의 리스크 질문을 빨리 뽑는 도구.

성공 기준:

- “AI 설문조사 대체”로 오해될 표현을 줄인다.
- 결과 상단에서 한계와 검증 필요 지점을 바로 볼 수 있다.
- 누가 포함된 패널인지 분포를 보여준다.
- 입력에 없는 가격/디자인/성별/착용상황 환각을 표시한다.
- 디자이너가 다음 설문이나 품평에서 확인할 질문을 얻는다.

## 릴리스 범위

### 포함

- 제품 포지션 문구 수정
- 사후 검증 플래그
- 패널 분포 표시
- 결과 상단 세줄요약
- 디자인 디테일 구조화 입력
- 스타일 톤 프리셋
- 동급 브랜드 대비 가격 위치 입력
- 베타 연령대 프리셋
- Web UI 연령 범위 슬라이더와 직접 입력
- 프롬프트 안전 규칙 강화
- 리포트/CSV에 검증 및 패널 분포 정보 추가

### 제외

- 이미지 업로드 직접 평가
- 실제 판매 데이터 검증
- 무신사 데이터 연동
- 구매율/매출/시장점유율 예측 표현
- 복잡한 가중 샘플링 UI

### 설계 포함, 구현 보류

- 옵션 토글형 `이미지 기반 컨셉 설명 보조`
- 목표 버전: `v0.8.0` 후보
- 원칙: 이미지는 페르소나 평가에 반복 투입하지 않는다. 이미지 분석 API는 컨셉 설명 초안을 만들 때 1회만 호출한다.

## 정부/공공 API 연결 순서

목표는 “AI가 더 똑똑해 보이게 만드는 것”이 아니라, 리포트가 공식 통계 기준선을 같이 보여주게 만드는 것이다. 모든 API는 결과 점수를 직접 보정하기보다 참고 맥락, 패널 분포, 검증 플래그, 리포트 주석에 먼저 사용한다.

### 공통 원칙

- API 실패 시 분석 실행을 막지 않는다. 실패한 API는 `참고 통계 미사용`으로 표시하고 기존 텍스트 기반 평가를 계속 진행한다.
- 모든 API 응답은 출처, 기준일, 갱신주기, 호출 성공 여부를 리포트에 남긴다.
- API 키는 환경변수로만 받는다. 공개 HF Space에는 키를 커밋하지 않는다.
- 응답 캐시를 둔다. 같은 통계표나 같은 지역 기준값은 매 실행마다 다시 호출하지 않는다.
- 정부 통계는 실제 구매 의향, 판매량, 매출 예측으로 표현하지 않는다.
- 무신사 데이터 연동은 공식 계약과 권한 확인 전까지 제외한다.

### 순차 연결 규칙

모든 API는 병렬로 한꺼번에 붙이지 않는다. 앞 단계의 connector, 인증, 캐시, 실패 처리, 출처 표시, fixture 테스트가 끝난 뒤 다음 API로 넘어간다.

1. 공통 API 기반을 먼저 만든다.
2. KOSIS OpenAPI를 먼저 연결해 기존 가격 부담 기준값을 안정화한다.
3. 행안부 주민등록 인구 API를 다음 슬라이스로 연결해 패널 분포 설명을 보강한다.
4. SGIS S-Open API를 연결해 지역 입력이 있을 때만 지역 통계 맥락을 붙인다.
5. 소상공인시장진흥공단 상가정보 API를 연결해 지역 기반 오프라인 상권 맥락을 붙인다.
6. 기상청 API를 연결해 시즌, 소재, 날씨 민감 상품에만 날씨 맥락을 붙인다.
7. 한국소비자원 참가격 API를 연결해 생활물가 참고값을 선택적으로 붙인다.
8. 한국은행 ECOS API를 마지막에 연결해 거시경제 참고 섹션으로 분리한다.

게이트 기준:

- 이전 API의 `성공`, `실패`, `API 키 없음`, `캐시 사용` 상태가 리포트에 표시되어야 다음 API를 시작한다.
- 이전 API가 실패해도 앱 실행과 리포트 생성이 유지되는 fixture 테스트가 있어야 한다.
- 새 API가 점수나 긍정률을 직접 보정하면 안 된다. 먼저 참고 섹션이나 검증 주석으로만 붙인다.
- HF Space 배포 환경에서는 API 키가 없어도 기본 분석이 동작해야 한다.
- API별 작업은 담당 에이전트가 다르더라도 merge 순서는 아래 순서를 따른다.

### 0단계: 공통 API 기반

담당: Public Data Platform Agent

작업:

- `src/public_data/` 계층을 만든다.
- API별 connector, cache, error, source metadata 구조를 통일한다.
- 리포트에 `공식 통계 참고값`, `출처`, `기준일`, `호출 상태`를 표시할 공통 모델을 만든다.
- 테스트용 fixture를 만들어 네트워크 없이 회귀 테스트 가능하게 한다.

완료 기준:

- API connector가 실패해도 앱 실행과 리포트 생성이 깨지지 않는다.
- 리포트에 API 성공/실패 상태가 명확히 남는다.

### 1단계: KOSIS OpenAPI 확장

담당: KOSIS Integration Agent

목표 버전: `v0.7.0`

용도:

- 의류·신발 지출 기준값
- 가구소득/처분가능소득 기준값
- 소비자물가지수 중 의류·신발 관련 지표
- 온라인쇼핑동향조사 중 의복, 신발, 가방, 패션 관련 상품군 거래액

리포트 반영:

- 가격 부담 해석의 기준값을 더 명확히 한다.
- “온라인 패션 소비 맥락” 섹션을 추가한다.
- 통계 기준 기간과 출처를 표에 표시한다.

주의:

- KOSIS 값은 집계 통계다. 개별 페르소나의 실제 소득이나 구매력을 추정했다고 쓰면 안 된다.

### 2단계: 행안부 주민등록 인구 API

담당: Population Data Agent

목표 버전: `v0.7.1`

용도:

- 연령별 인구 분포 참고
- 성별 인구 분포 참고
- 지역별 인구 분포 참고
- Web UI 연령 범위 슬라이더의 선택 범위와 실제 표본 분포 비교

리포트 반영:

- 선택한 타깃 연령 범위가 전체 인구 기준에서 어느 범위인지 표시한다.
- 합성 패널의 연령/성별/지역 분포와 주민등록 인구 통계가 다를 수 있음을 표시한다.

주의:

- 주민등록 인구는 소비자 구매력 데이터가 아니다. 패널 구성 설명과 표본 한계 설명에만 사용한다.

### 3단계: SGIS S-Open API

담당: Spatial Statistics Agent

목표 버전: `v0.8.0`

용도:

- 지역 기반 인구·가구·공간통계 참고
- 특정 지역 타깃 입력 시 지역 맥락 보강
- 향후 지도 기반 리포트의 기반 데이터

리포트 반영:

- 지역 타깃이 있을 때만 “지역 통계 맥락” 섹션을 표시한다.
- 전국 타깃이면 호출하지 않는다.

주의:

- 지도 UI는 v0.8.0에서도 필수 범위가 아니다. 먼저 표 기반 리포트로 붙인다.

### 4단계: 소상공인시장진흥공단 상가(상권)정보 API

담당: Commercial Area Data Agent

목표 버전: `v0.8.0`

용도:

- 지역별 의류·잡화·소매 업종 밀도 참고
- 오프라인 판매 가능성 자체가 아니라 “오프라인 경쟁/입지 맥락” 참고

리포트 반영:

- 지역 입력이 있을 때 해당 지역의 관련 업종 수 또는 밀도를 참고값으로 표시한다.
- “판매량 예측 아님” 문구를 고정 표시한다.

주의:

- 상가 수가 많다고 수요가 높다고 단정하면 안 된다. 경쟁 밀도와 입지 맥락으로만 쓴다.

### 5단계: 기상청 단기예보/기상자료 API

담당: Weather Context Agent

목표 버전: `v0.8.0`

용도:

- 시즌, 소재, 아우터, 레이어링, 장마, 폭염, 한파 관련 맥락 보조
- 제품 시즌 입력과 실제 날씨 시즌성의 충돌 여부 참고

리포트 반영:

- 시즌성 강한 상품일 때만 “날씨/시즌 맥락” 섹션을 표시한다.
- 예보값은 실시간성이 있으므로 기준 시각을 표시한다.

주의:

- 날씨는 컨셉 적합성의 보조 신호다. 페르소나 반응 점수를 직접 바꾸지 않는다.

### 6단계: 한국소비자원 참가격 API

담당: Consumer Price Data Agent

목표 버전: `v0.9.0`

용도:

- 일반 생활물가 참고
- 가격 부담 해석의 주변 경제 맥락 보조

리포트 반영:

- 의류 직접 가격 비교가 아니라 생활물가 참고값으로만 표시한다.
- 의류 가격 기준으로 오해될 경우 섹션을 숨긴다.

주의:

- 참가격은 생필품 중심이라 K-Fashion 컨셉 평가와 직접 관련성이 낮다. 우선순위는 낮다.

### 7단계: 한국은행 ECOS API

담당: Macro Context Agent

목표 버전: `v0.9.0`

용도:

- 소비심리, 물가, 금리, 경기 맥락 참고
- 패션 상품 평가가 아니라 시장 배경 설명 보조

리포트 반영:

- “거시경제 참고” 섹션으로 분리한다.
- 기본 리포트에서는 접고, 상세 리포트에서만 표시한다.

주의:

- 한국은행은 정부 API는 아니지만 공신력 있는 공공 경제통계다. 정부 데이터와 같은 묶음으로 말할 때는 “정부/공공 통계”로 표현한다.

### API 연결 우선순위 요약

| 순서 | API | 목표 버전 | 담당 에이전트 | 리포트 영향 | 필수 여부 |
|---:|---|---|---|---|---|
| 0 | 공통 API 기반 | `v0.7.0` | Public Data Platform Agent | 출처/캐시/실패 처리 공통화 | 필수 |
| 1 | KOSIS OpenAPI | `v0.7.0` | KOSIS Integration Agent | 가격·소비·온라인쇼핑 기준값 | 필수 |
| 2 | 행안부 주민등록 인구 API | `v0.7.1` | Population Data Agent | 연령·성별·지역 패널 설명 | 필수 |
| 3 | SGIS S-Open API | `v0.8.0` | Spatial Statistics Agent | 지역 통계 맥락 | 선택 |
| 4 | 소상공인 상가(상권)정보 API | `v0.8.0` | Commercial Area Data Agent | 오프라인 상권 맥락 | 선택 |
| 5 | 기상청 기상 API | `v0.8.0` | Weather Context Agent | 시즌·소재 맥락 | 선택 |
| 6 | 한국소비자원 참가격 API | `v0.9.0` | Consumer Price Data Agent | 생활물가 참고 | 낮음 |
| 7 | 한국은행 ECOS API | `v0.9.0` | Macro Context Agent | 거시경제 참고 | 낮음 |

## 워크스트림 분해

| ID | 워크스트림 | 담당 에이전트 역할 | 난이도 | 주요 산출물 | 주요 파일 후보 |
|---|---|---|---:|---|---|
| WS-0 | 제품 포지션/문구 정리 | PM Copy Agent | 낮음 | UI/README/리포트 한계 문구 통일 | `src/ui/copy.py`, `README.md`, `docs/README-ENG.md`, `docs/INSTALL.md` |
| WS-1 | 사후 검증 레이어 | Backend Validation Agent | 높음 | 가격/디자인/성별/착용상황 검증 플래그 | `src/result_parser.py`, `src/aggregator.py`, `src/report_writer.py`, `tests/` |
| WS-2 | 패널 분포 표시 | Data Reporting Agent | 중간 | 성별/연령/직업/지역/실제 샘플 수 표시 | `src/aggregator.py`, `src/ui/rendering.py`, `src/report_writer.py` |
| WS-3 | 결과 상단 세줄요약 | Report UX Agent | 중간 | 반응 방향/긍정 요인/검증 필요 요약 | `src/aggregator.py`, `src/ui/rendering.py`, `src/report_writer.py` |
| WS-4 | 디자인 디테일 입력 | Input UX Agent | 중간 | 체크박스와 자유 입력, “없음” 명시 | `src/app_config.py`, `src/ui/rendering.py`, `src/prompt_builder.py` |
| WS-5 | 타깃/가격 맥락 입력 | Input Strategy Agent | 중간 | 연령대 프리셋, 동급 브랜드 가격 위치 | `src/persona_filter.py`, `src/ui/rendering.py`, `src/prompt_builder.py` |
| WS-5A | 연령 범위 세부 UI | Input UX Agent | 중간 | 하한/상한 연령 슬라이더, 직접 입력, 10단위 스텝과 세부 입력 동기화 | `src/ui/rendering.py`, `src/persona_filter.py`, `tests/test_app.py` |
| WS-6 | 프롬프트 강화 | Prompt Agent | 낮음 | 가격/디자인/착용상황/성별 안전 규칙 | `prompts/concept_eval_ko_v0_3.md`, `tests/test_prompt_builder.py` |
| WS-7 | QA/회귀 테스트 | QA Agent | 높음 | 오해 방지, 검증 플래그, 리포트 출력 회귀 테스트 | `tests/fixtures/`, `tests/test_app.py`, `tests/test_report_writer.py` |
| WS-8 | 릴리스 관리 | Release Agent | 낮음 | 버전 `0.7.0`, changelog, 배포 확인 | `pyproject.toml`, `src/app_config.py`, `README.md`, `docs/CHANGELOG.md` |
| WS-9 | 이미지 보조 기능 설계 | Image Assist PM Agent | 중간 | 토글형 이미지 설명 보조 정책, 경고 문구, API 호출 경계 | `update-PLAN.md`, `docs/`, 이후 `src/ui/rendering.py` |
| WS-10 | 정부/공공 API 공통 기반 | Public Data Platform Agent | 높음 | API connector/cache/source/error 공통 구조 | `src/public_data/`, `src/app_config.py`, `tests/fixtures/` |
| WS-11 | KOSIS API 1차 연결 | KOSIS Integration Agent | 높음 | KOSIS 확장, 가격·소비·온라인쇼핑 기준값, 리포트 반영 | `src/economic_context.py`, `src/report_writer.py`, `src/ui/rendering.py`, `tests/` |
| WS-11A | 행안부 API 2차 연결 | Population Data Agent | 높음 | 주민등록 인구 API 연결, 연령·성별·지역 기준 분포 비교 | `src/public_data/`, `src/report_writer.py`, `src/ui/rendering.py`, `tests/` |
| WS-12 | SGIS/상권/기상 API 3차 연결 | Context Data Agent | 높음 | 지역·상권·날씨 맥락 선택 섹션 | `src/public_data/`, `src/aggregator.py`, `src/report_writer.py` |
| WS-13 | 참가격/ECOS API 4차 연결 | Macro Data Agent | 중간 | 생활물가·거시경제 참고 섹션 | `src/public_data/`, `src/report_writer.py`, `docs/` |

## 실행 순서

### 0단계: 기대치 조정

담당: PM Copy Agent

작업:

- “AI 설문조사”, “구매율 예측”, “판매 가능성 예측” 느낌의 표현 제거.
- “합성 페르소나 기반 사전 리스크 점검”으로 문구 통일.
- 로그인/입력/결과/리포트 푸터에 한계 문구 반복 배치.

권장 고정 문구:

```text
이 결과는 실제 소비자 조사나 판매 예측이 아니라, 합성 페르소나 기반의 사전 리스크 점검입니다.
```

완료 기준:

- README와 앱 UI에서 “실제 조사 대체”로 읽히는 문구가 없다.
- 리포트 첫 화면에서 한계 문구를 바로 확인할 수 있다.

### 1단계: 결과 신뢰 방어선

담당: Backend Validation Agent, Data Reporting Agent, Report UX Agent

작업:

- 사후 검증 플래그 추가.
- 패널 분포 표시.
- 결과 상단 세줄요약 추가.
- 심각한 오류는 상단 요약에서 별도 “검증 필요”로 표시.

검증 플래그 초안:

- 입력 가격 불일치 가능
- 미입력 디자인 요소 언급 가능
- 성별 맥락 불일치 가능
- 착용 상황 불일치 가능

완료 기준:

- LLM 결과를 무조건 버리지 않고 “검증 필요”로 라벨링한다.
- 결과 상단에 `먼저 볼 요약`이 생긴다.
- 리포트와 UI에서 패널 분포를 볼 수 있다.

### 2단계: 입력 구조 개선

담당: Input UX Agent, Input Strategy Agent, Prompt Agent

작업:

- 디자인 디테일 체크박스 추가.
- 디자인 디테일 자유 입력 추가.
- 스타일 톤 프리셋 추가.
- 동급 브랜드 대비 가격 위치 입력 추가.

디자인 디테일 체크박스:

- 그래픽 있음
- 패턴 있음
- 로고 있음
- 자수 있음
- 프린트 있음
- 워싱/가공 있음
- 장식 없음

동급 브랜드 대비 가격 위치:

- 저렴한 편
- 비슷한 편
- 비싼 편
- 모르겠음

완료 기준:

- “없음”이 구조화 입력으로 남는다.
- 프롬프트에 입력되지 않은 디자인 요소를 만들지 말라는 규칙이 들어간다.
- 리포트에서 공공 통계 기준 가격 부담과 동급 브랜드 기준 가격 위치를 분리해 보여준다.

### 3단계: 패널 제어 개선

담당: Input Strategy Agent, Input UX Agent, Data Reporting Agent

작업:

- 베타 연령대 프리셋 추가.
- Web UI 연령 범위 세부 설정 추가.
- 필터 조건과 실제 샘플 수 표시.
- 표본 부족 시 인접 연령 보조 포함 정책 설계.
- 직업 라벨은 원문 기준임을 명시.

연령대 프리셋:

- 전체
- 10대부터 20대
- 20대부터 30대
- 30대부터 40대
- 40대 이상

연령 범위 세부 UI:

- 하한 연령과 상한 연령을 각각 조절할 수 있게 한다.
- 기본 조작은 10단위 스텝으로 움직인다.
- 옆의 숫자 입력 필드에 직접 적으면 1세 단위까지 세부 설정할 수 있다.
- 직접 입력값은 슬라이더 상태와 동기화한다.
- 하한이 상한보다 커지면 즉시 보정하거나 오류 문구를 보여준다.
- 프리셋을 누르면 슬라이더/직접 입력값이 같이 바뀐다.

권장 UI 구조:

```text
타깃 연령 범위
[하한 슬라이더]  [숫자 입력]
[상한 슬라이더]  [숫자 입력]
```

대안:

- 가능하면 범위 슬라이더 1개를 우선 검토한다.
- 단, Streamlit 구현 제약이나 모바일 조작성 문제가 있으면 하한/상한 슬라이더 2개로 간다.

완료 기준:

- 사용자가 “누구에게 물었는지”를 결과에서 바로 확인한다.
- 표본 부족 보정이 발생하면 리포트에 명시된다.
- 연령 필터가 프리셋과 세부 입력 모두에서 같은 `PersonaFilter`로 전달된다.
- 리포트에 선택 연령 범위와 실제 포함 연령 분포가 같이 표시된다.

### 3A단계: 정부/공공 API 1차 연결

담당: Public Data Platform Agent, KOSIS Integration Agent

작업:

- 공통 API connector/cache/source/error 구조를 먼저 만든다.
- KOSIS OpenAPI를 확장해 의류·신발 지출, 소득, 물가, 온라인 패션 거래액 참고값을 가져온다.
- 행안부 주민등록 인구 API는 `v0.7.1`로 분리하고, KOSIS 연결이 안정화된 뒤 진행한다.
- API 호출 실패 시 앱 실행과 리포트 생성을 유지한다.
- SGIS, 상권정보, 기상청, 참가격, ECOS는 후속 버전 이슈로 분리한다.

완료 기준:

- `v0.7.0`에서 공통 API 기반과 KOSIS API가 우선 연결된다.
- 리포트에 API 출처, 기준일, 호출 성공 여부가 표시된다.
- API가 실패해도 기존 합성 페르소나 분석은 계속 진행된다.
- 후속 API 연결 순서가 `v0.7.1`, `v0.8.0`, `v0.9.0` 작업으로 남는다.

### 4단계: UI 품질 및 배포 안정화

담당: QA Agent, Release Agent

작업:

- 다크모드 가시성 확인.
- 리포트 생성/다운로드 상태 표시 확인.
- 오류 메시지와 재시도 흐름 점검.
- `0.7.0` 버전 반영.
- GitHub와 HF Space 배포 확인.

완료 기준:

- 주요 Streamlit 흐름이 로컬 테스트에서 통과한다.
- README, 앱 버전, changelog가 모두 `0.7.0`으로 맞다.
- HF Space에서 README와 앱이 같은 배포 상태를 보인다.

### 5단계: 이미지 기반 컨셉 설명 보조 설계

담당: Image Assist PM Agent

상태:

- `v0.7.0` 구현 범위는 아니다.
- `v0.8.0` 후보로 설계만 확정한다.

권장 구조:

```text
토글 OFF
-> 기존 텍스트 입력만으로 진행

토글 ON
-> 이미지 업로드
-> 이미지 분석 API 1회 호출
-> 컨셉 설명 초안 생성
-> 사용자가 수정/확정
-> 확정된 텍스트만 페르소나 평가에 사용
```

제품 원칙:

- 기본값은 OFF.
- 이미지를 100명 페르소나 평가마다 반복 전송하지 않는다.
- 이미지는 “평가 대상”이 아니라 “컨셉 설명 초안 작성 보조”에만 쓴다.
- 사용자가 분석 결과를 수정/확정하기 전에는 실행하지 않는다.
- 업로드 이미지는 저장하지 않는 것을 기본 정책으로 둔다.
- 공개 HF Space에서는 더 강한 경고와 민감 디자인 업로드 금지 문구를 둔다.

권장 UI 명칭:

```text
이미지 기반 컨셉 설명 보조
```

권장 경고 문구:

```text
이미지는 외부 AI API로 전송되어 컨셉 설명 초안을 만드는 데 사용됩니다.
이미지는 페르소나 평가에 반복 사용되지 않습니다.
생성된 설명은 실행 전 직접 수정·확정해야 합니다.
출시 전 민감한 디자인이나 비공개 샘플은 업로드하지 마세요.
```

하지 말아야 할 구조:

- 이미지 1장을 페르소나 100명 평가에 매번 같이 보내는 구조.
- 이미지 분석 결과를 사용자 확인 없이 바로 평가에 쓰는 구조.
- “이미지 평가”처럼 실제 상품 이미지를 직접 검증하는 표현.

완료 기준:

- 기능 요구사항 문서에 API 호출 횟수, 저장 정책, 사용자 확인 단계를 명시한다.
- `v0.7.0` 구현 작업과 섞이지 않는다.
- `v0.8.0` 시작 전 비용, 개인정보, 출시 전 디자인 유출 리스크를 다시 검토한다.

## 에이전트별 작업 카드

### PM Copy Agent

책임:

- 제품 정의와 금지 표현 정리.
- UI/README/리포트 문구 톤 통일.

작업 규모:

- 파일 3개부터 5개.
- 코드 로직 변경 거의 없음.

주의:

- “예측”, “검증 완료”, “실제 고객 반응 대체” 표현 금지.

### Backend Validation Agent

책임:

- LLM 출력 후 검증 로직 설계와 구현.
- 가격/디자인/성별/착용상황 플래그 생성.

작업 규모:

- 신규 모듈 1개 또는 기존 parser/aggregator 확장.
- 테스트 필수.

주의:

- 처음부터 결과를 버리지 말고 라벨링 중심으로 구현.
- false positive를 줄이기 위해 “가능” 표현 사용.

### Data Reporting Agent

책임:

- 이번 실행 패널 분포 집계.
- UI와 리포트에 분포 표시.

작업 규모:

- aggregator/report_writer/rendering 수정.
- 기존 `persona_attributes` 활용.

주의:

- 실제 고객 분포가 아니라 합성 페르소나 분포임을 고정 표시.

### Report UX Agent

책임:

- 결과 상단 세줄요약 설계.
- 리포트 구조를 읽기 쉬운 순서로 조정.

작업 규모:

- 리포트 writer와 UI 일부 수정.
- LLM 재호출 없음.

주의:

- 새 모델 호출로 요약하지 않는다. 기존 집계값 기반으로 만든다.

### Input UX Agent

책임:

- 디자인 디테일 입력 UI.
- “장식 없음” 같은 부정 입력을 구조화.
- 연령 범위 슬라이더와 직접 숫자 입력 UI.

작업 규모:

- app_config/rendering/prompt_builder 수정.
- 연령 UI는 rendering/persona_filter/test_app 수정 가능.

주의:

- 이미지 업로드로 확장하지 않는다.
- 하한/상한 연령이 꼬이지 않게 입력 검증을 둔다.

### Image Assist PM Agent

책임:

- 이미지 기반 컨셉 설명 보조 기능의 제품 정책 설계.
- 토글 OFF 기본값, 경고 문구, API 호출 경계, 저장 정책 정의.

작업 규모:

- 문서 중심.
- `v0.7.0`에서는 구현하지 않는다.

주의:

- 이미지 직접 평가 기능으로 포장하지 않는다.
- 페르소나 평가 호출마다 이미지를 보내는 구조를 금지한다.
- 사용자가 확인한 텍스트만 평가 입력으로 넘기는 구조를 유지한다.

### Input Strategy Agent

책임:

- 연령대 프리셋, 가격 위치 입력, 표본 부족 보정 정책.

작업 규모:

- persona_filter와 UI 수정.
- 표본 부족 테스트 필요.

주의:

- 가중 샘플링 UI는 이번 범위에서 제외.

### Prompt Agent

책임:

- 프롬프트 안전 규칙 추가.
- 입력 외 요소 생성 금지 규칙 반영.

작업 규모:

- prompt 파일 1개, 관련 테스트.

주의:

- 프롬프트만으로 해결된다고 가정하지 않는다. 사후 검증과 같이 간다.

### QA Agent

책임:

- 회귀 테스트.
- 대표 실패 케이스 fixture 구성.

작업 규모:

- 테스트 여러 개.
- UI 렌더링 테스트는 가능한 범위에서 최소화.

주의:

- 가격 오독, 미입력 그래픽 언급, 성별 맥락 오류 케이스를 반드시 넣는다.

### Release Agent

책임:

- 버전 `0.7.0` 반영.
- changelog 작성.
- GitHub/HF Space 동기화.

작업 규모:

- 버전 파일과 문서.

주의:

- HF Space는 이미지/Xet 포인터 차이 때문에 GitHub와 커밋 SHA가 달라도 된다. 콘텐츠 기준으로 확인한다.

### Public Data Platform Agent

책임:

- 정부/공공 API 공통 연결 기반 설계.
- connector, cache, error, source metadata 구조 통일.

작업 규모:

- 신규 `src/public_data/` 계층.
- 네트워크 없는 fixture 테스트.

주의:

- 특정 API 하나에 맞춘 구조로 만들지 않는다.
- API 실패가 전체 분석 실패로 번지지 않게 한다.

### KOSIS Integration Agent

책임:

- KOSIS OpenAPI 확장 연결.
- 의류·신발 지출, 소득, 물가, 온라인 패션 거래액 참고값 정리.

작업 규모:

- KOSIS connector와 통계표 매핑.
- 리포트 참고 통계 표 확장.

주의:

- KOSIS는 집계 통계다. 개별 페르소나의 실제 구매력으로 해석하지 않는다.

### Population Data Agent

책임:

- 행안부 주민등록 인구 API 연결.
- 연령·성별·지역 기준 분포를 패널 설명에 반영.

작업 규모:

- 인구 API connector.
- 연령 슬라이더 선택값과 인구 기준 분포 비교.

주의:

- 인구 분포는 구매 의향이 아니다. 표본 설명과 한계 설명에만 쓴다.

### Spatial Statistics Agent

책임:

- SGIS S-Open API 연결 설계와 구현.
- 지역 타깃 입력이 있을 때 공간통계 맥락을 제공.

작업 규모:

- `v0.8.0` 선택 기능.
- 지도보다 표 기반 리포트 우선.

주의:

- 전국 타깃이면 호출하지 않는다.

### Commercial Area Data Agent

책임:

- 소상공인시장진흥공단 상가(상권)정보 API 연결.
- 의류·잡화·소매 업종 밀도 참고값 제공.

작업 규모:

- `v0.8.0` 선택 기능.
- 지역 입력이 있을 때만 동작.

주의:

- 상가 수를 수요나 매출 예측으로 해석하지 않는다.

### Weather Context Agent

책임:

- 기상청 API 연결.
- 시즌, 소재, 착용상황 맥락 보조.

작업 규모:

- `v0.8.0` 선택 기능.
- 시즌성 강한 상품일 때만 리포트에 표시.

주의:

- 날씨 데이터로 페르소나 반응 점수를 직접 바꾸지 않는다.

### Consumer Price Data Agent

책임:

- 한국소비자원 참가격 API 연결 검토와 구현.
- 생활물가 참고값 제공.

작업 규모:

- `v0.9.0` 낮은 우선순위 기능.

주의:

- 의류 직접 가격 비교 데이터처럼 보이면 섹션을 숨긴다.

### Macro Context Agent

책임:

- 한국은행 ECOS API 연결.
- 소비심리, 물가, 금리, 경기 맥락 참고값 제공.

작업 규모:

- `v0.9.0` 낮은 우선순위 기능.
- 상세 리포트 전용 섹션.

주의:

- 정부 API가 아니라 공공 경제통계로 표현한다.

## 우선순위

| 우선순위 | 항목 | 이유 |
|---:|---|---|
| P0 | 제품 포지션 문구 수정 | 오해를 줄이지 않으면 이후 기능도 “정확도 실패”로 읽힌다. |
| P0 | 사후 검증 플래그 | 틀린 결과를 앱이 그대로 보여주는 문제를 막는다. |
| P1 | 정부/공공 API 공통 기반 | API 실패, 출처, 기준일, 캐시 처리를 먼저 통일해야 뒤 API가 꼬이지 않는다. |
| P1 | KOSIS API 1차 연결 | 가격·소비 기준값을 먼저 안정화해야 리포트 기준선이 잡힌다. |
| P1 | 패널 분포 표시 | “누구에게 물었는지” 불신을 줄인다. |
| P1 | 세줄요약 | 디자이너가 긴 리포트 전에 핵심을 본다. |
| P2 | 행안부 API 2차 연결 | 연령·성별·지역 패널 설명은 중요하지만 KOSIS 안정화 뒤 붙이는 게 충돌이 적다. |
| P2 | 디자인 디테일 입력 | 패션 컨셉 평가의 핵심 맥락을 보강한다. |
| P2 | 연령대 프리셋 | 베타 사용자에게 필요한 최소 패널 제어를 준다. |
| P2 | 연령 범위 세부 UI | 프리셋보다 정밀한 타깃 설정을 원하는 사용자를 지원한다. |
| P3 | 스타일 톤/가격 위치 | 평가 기준 보조 맥락을 개선한다. |
| P3 | SGIS/상권/기상 API 2차 연결 | 지역, 오프라인, 시즌 맥락은 유용하지만 기본 분석을 막는 필수 요소는 아니다. |
| P4 | 이미지 기반 컨셉 설명 보조 설계 | 비용과 유출 리스크가 있으므로 `v0.8.0` 후보로 문서화만 한다. |
| P4 | 참가격/ECOS API 3차 연결 | 직접 패션 정확도보다 생활물가·거시경제 참고 성격이 강하다. |

## 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 검증 플래그가 너무 많이 뜸 | “오류”가 아니라 “검증 필요 가능성”으로 표현한다. |
| 패널 분포가 실제 고객처럼 오해됨 | 모든 분포 표에 “합성 페르소나 기준” 문구를 붙인다. |
| 디자인 디테일 입력이 복잡해짐 | 체크박스 7개와 자유 입력 1개로 제한한다. |
| 연령 필터로 표본 부족 | 인접 연령 보조 포함과 안내 문구를 둔다. |
| 연령 슬라이더와 직접 입력값이 충돌함 | 단일 상태값을 기준으로 동기화하고 하한/상한 역전 검증을 둔다. |
| 프롬프트 강화만 믿게 됨 | 사후 검증을 P0로 유지한다. |
| 이미지 업로드가 평가 기능으로 오해됨 | 기능명을 “이미지 기반 컨셉 설명 보조”로 제한하고 기본값 OFF로 둔다. |
| 이미지 비용이 과도해짐 | 이미지 분석 API는 초안 생성 1회만 호출하고 페르소나 평가에는 텍스트만 사용한다. |
| 출시 전 디자인 유출 우려 | 저장하지 않음, 외부 API 전송 고지, 민감 디자인 업로드 금지 문구를 둔다. |

## 릴리스 체크리스트

- [ ] `pyproject.toml` 버전 `0.7.0`
- [ ] `src/app_config.py` `APP_VERSION = "0.7.0"`
- [ ] README 버전 배지 `0.7.0`
- [ ] `docs/README-ENG.md` 버전 배지 `0.7.0`
- [ ] `docs/CHANGELOG.md`에 `v0.7.0` 항목 추가
- [ ] 리포트 푸터 한계 문구 확인
- [ ] 패널 분포 UI 확인
- [ ] 연령 범위 슬라이더/직접 입력 동기화 확인
- [ ] 정부/공공 API 공통 기반 추가
- [ ] KOSIS OpenAPI 확장 연결 확인
- [ ] 행안부 주민등록 인구 API는 `v0.7.1` 순차 연결 작업으로 분리
- [ ] API 실패 시 앱 실행과 리포트 생성 유지 확인
- [ ] API 출처, 기준일, 호출 상태 리포트 표시 확인
- [ ] 모든 후속 API는 공통 기반, KOSIS, 행안부, SGIS, 상권정보, 기상청, 참가격, ECOS 순서로 연결
- [ ] SGIS/상권/기상 API는 `v0.8.0` 후속 이슈로 분리
- [ ] 참가격/ECOS API는 `v0.9.0` 후속 이슈로 분리
- [ ] 검증 플래그 테스트 통과
- [ ] 리포트 예시 최신화
- [ ] GitHub `main` push
- [ ] HF Space 업로드 및 README 렌더 확인
