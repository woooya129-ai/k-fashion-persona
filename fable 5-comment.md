# fable 5-comment — 사용성 개선 제안 (v0.8.0 기준)

> 작성일: 2026-06-12 / 대상: k-fashion-persona 작업자
> 배경: 사용자 공통 피드백 "너무 어렵다".
> 전제: 이 도구의 본질은 **텍스트 정보만으로 빠르게 스크리닝**하는 것.
> 이미지는 스케치/제품컷이 있는 사용자를 위한 **선택 옵션(토글 ON/OFF)**으로만 두고,
> 텍스트 경로 자체를 쉽게 만드는 것이 핵심 과제.

---

## 1. 왜 어렵다고 느끼는가 — 현황 진단

코드 기준으로 사용자가 첫 리포트를 받기까지 거쳐야 하는 단계:

| 단계 | 현재 요구사항 | 부담 포인트 |
|---|---|---|
| 1. 모델/키 설정 | provider 선택 + API key 직접 발급·붙여넣기 (`render_simple_setup`, rendering.py:1523) | 비개발자는 API key 발급 자체가 최대 장벽 |
| 2. 컨셉 입력 | 카테고리, 가격, 핏, 소재, 컬러, 디자인 디테일, 시즌, 착용 상황, 스타일 톤, 타깃 가설, 제품 설명(3000자) — **11개 필드 전부 빈 칸에서 직접 타이핑** (`render_concept_inputs`, rendering.py:1112) | 무엇을 어느 수준으로 써야 할지 가이드가 없음. 핏/소재/스타일 톤은 자유 텍스트라 막막함 |
| 3. 실행 확인 | 비용 확인 체크박스 + ENTER (app.py:728) | 토큰/USD 비용 개념이 낯선 사용자에게 불안 요소 |
| 4. 결과 해석 | 20개 이상 섹션의 전문가용 리포트 | 첫 사용자가 "그래서 결론이 뭔데?"를 찾기 어려움 |

**결론: "어렵다"의 본질은 ① 입력 필드가 많고 전부 자유 타이핑, ② 실행 전 준비(키)가 무겁고, ③ 결과가 길다 — 세 가지. 이미지가 없어서가 아니라 텍스트 입력 경험 자체가 무겁다.**

---

## 2. 개선 방안 — 텍스트 경로 중심 (우선순위 순)

### P1. 자유 텍스트 → 필드 자동 분해 ("붙여넣고 시작")

가장 효과 큰 변경. 이미지 없이 **텍스트만으로** 입력 부담을 없앤다.

- 입력 화면 최상단에 큰 텍스트 박스 1개: *"제품을 아는 대로 적거나, 상세페이지 문구를 붙여넣으세요"*
- LLM 1회 호출로 11개 필드를 JSON으로 자동 분해 → 각 필드에 채워 넣고 **사용자가 수정 가능한 상태**로 보여줌
- 쇼핑몰 상세설명, 기획서 한 단락, 메모 수준의 한 줄("가을용 12만원대 세미오버핏 코튼 셔츠") 전부 입력으로 허용
- 기존 이미지 보조와 같은 draft 패턴이므로 안전 경계 변화 없음 (텍스트 in → 텍스트 out)
- 못 찾은 필드는 빈 값으로 두고 추측 금지 → 빈 필드는 기존 `PRODUCT_CARD_EMPTY_PLACEHOLDER` 처리 경로 그대로 활용

### P2. 자유 입력 필드를 선택지(칩/드롭다운)로 전환

핏, 소재, 컬러, 시즌, 착용 상황은 패션 도메인에서 보기 항목이 거의 정해져 있다.

- 핏: 슬림 / 레귤러 / 세미오버 / 오버 + 직접 입력
- 시즌: 봄·가을 / 여름 / 겨울 / 사계절 (멀티 선택)
- 착용 상황: 출근 / 데일리 / 모임 / 운동 / 행사 + 직접 입력
- 소재·컬러: 자주 쓰는 상위 옵션 + 직접 입력
- 스타일 톤은 이미 프리셋 selectbox(`STYLE_TONE_PRESETS`)가 있음 — 같은 패턴을 확장하는 것
- 타이핑 → 클릭으로 바뀌면 입력 시간과 "뭘 써야 하지" 고민이 동시에 줄어듦

### P3. 필수 3개만 노출, 나머지는 "선택 입력" 접기

- 실행 가드(app.py:746-757)는 이미 `description + category + key`만 필수 — **UI도 그렇게 보이게**
- 1단: 카테고리 / 가격 / 제품 설명 (필수 표시)
- 2단: 나머지 필드는 "더 정확한 결과를 원하면 추가 입력" expander로
- "필수 3개만 채워도 실행 가능"이 보이는 순간 심리적 장벽이 크게 내려감

### P4. 예시 프리셋으로 시작하기 (Empty-state 해소)

- "예시 컨셉 불러오기" 버튼: README의 출근 셔츠 예시 등 2~3개 프리셋으로 전 필드 채움
- 신규 사용자가 "완성된 입력이 어떤 모습인지"를 보고 배우는 효과
- 구현 비용 거의 없음: 프리셋 dict → session_state 주입 버튼 하나

### P5. 결과 리포트 "3줄 결론" 우선 노출

- 최상단 요약 카드: 긍정률 / 최대 리스크 1개 / 다음 액션 1개
- 지역별·직업별 등 상세 테이블은 expander로 접기 — 데이터는 그대로, 첫 화면만 단순화
- `build_run_report` 출력은 유지, 렌더링 레이어(`_render_job_panel_impl`, app.py:520)만 수정

### P6. 미니 프리뷰 모드 (비용 불안 해소)

- "5명 미리보기" 버튼: FAST(50명)보다 작은 5명 샘플로 수 초·수 센트 수준 체험
- `RUN_MODE_PRESETS`에 `preview` 프리셋 추가만으로 가능 (app_config.py)
- 입력 품질을 결과로 즉시 피드백 받는 루프 → 본 실행 전에 입력을 다듬게 됨

### P7. 이미지 보조 — 옵션 유지 + 켰을 때만 강화

이미지는 **기본 OFF 토글 유지** (현재 구조 그대로, rendering.py:1050). 텍스트 경로의 보조 수단일 뿐.

- 켰을 때 개선 ①: 현재 `description` 한 필드만 채우는데(image_assist.py:61), P1과 같은 JSON 분해를 적용해 핏/소재/컬러 등도 draft로 제안
- 켰을 때 개선 ②: env 변수 3개 요구(image_assist.py:111) 대신 **사용자가 이미 입력한 세션 API key 재사용** — 지금은 env 설정 없으면 토글을 켜도 "사용 불가" 경고만 떠서 사실상 죽은 기능
- 원본 이미지 비저장·평가 루프 텍스트 한정이라는 기존 안전 경계는 그대로

---

## 3. 텍스트 기반일 때의 대처법 (입력이 빈약하거나 애매할 때)

텍스트만으로 굴러가려면 "정보가 부족한 입력"을 어떻게 받아주느냐가 품질을 좌우한다.

| 상황 | 대처 | 구현 위치 |
|---|---|---|
| 한 줄짜리 입력 ("가을 셔츠 12만원") | P1 자동 분해 → 채워진 필드만 사용, 빈 필드는 placeholder 처리(추측 금지). 실행은 막지 않되 "입력이 적으면 결과 신뢰도가 낮아져요" 안내 배지 | prompt_builder의 기존 빈 필드 처리 + UI 배지 |
| 상세페이지 통째로 붙여넣기 (3000자 초과·홍보 문구 섞임) | 자동 분해 시 "사실 정보만 추출, 광고 수사는 제외" 지시. 분해 결과를 보여주고 원문은 버림 | P1 프롬프트 |
| 모순된 입력 (시즌=여름인데 소재=울) | 분해/입력 직후 가벼운 정합성 체크 → 경고만 표시, 차단하지 않음. 기존 injection 경고 패턴(app.py:719) 재사용 | 신규 validate 함수 |
| 필드는 채웠는데 description이 빈 경우 | description은 필수이므로, 채워진 필드로 description 초안을 역생성하는 "설명 초안 만들기" 버튼 제공 (LLM 1회) | image_assist의 draft 패턴 재사용 |
| 자동 분해 결과가 틀린 경우 | draft는 항상 수정 가능한 입력란에 들어가므로 사용자가 고치면 됨. "자동 채움 값입니다 — 확인해 주세요" 표시로 맹신 방지 | UI 라벨 |
| LLM 분해 호출 실패 / JSON 파싱 실패 | 원문을 description에 그대로 넣고 수동 입력으로 fallback — 기능이 죽어도 기존 수동 경로는 항상 살아 있어야 함 | graceful degradation |

핵심 원칙: **자동화는 전부 "draft 제안"이고, 평가 루프에는 사용자가 확인한 텍스트만 들어간다.** 이미지 보조에서 이미 확립한 경계를 텍스트 자동 분해에도 동일하게 적용하면 된다.

---

## 4. 구현 팁 (작업자용 빠른 가이드)

### 4-1. 텍스트 자동 분해 (P1) — 신규 `concept_parser.py` 권장

image_assist.py와 같은 모듈 패턴(호출 경계 + draft 반환)으로:

```python
CONCEPT_PARSE_PROMPT = (
    "아래 패션 제품 텍스트에서 사실 정보만 추출해 JSON으로만 출력해. "
    "평가/반응 예측 금지, 광고 수사 제외, 확신 없는 필드는 빈 문자열.\n"
    '{"category": "", "fit": "", "material": "", "color": "", '
    '"design_details": [], "season": "", "occasion": "", '
    '"style_tone": "", "target_hypothesis": "", "description": ""}'
)
```

- 호출은 사용자가 입력한 세션 API key + 선택된 모델 그대로 사용 (`llm_client.py`의 기존 호출 경로 재사용 가능 — vision 불필요하므로 모든 provider에서 동작)
- `json.loads` 실패 시: 코드펜스 제거 후 재시도 → 그래도 실패하면 원문을 description에만 넣는 fallback

### 4-2. 필드 자동 채움 → session_state 주입

화이트리스트 방식으로만 주입:

```python
_FIELD_TO_STATE_KEY = {
    "category": "kfps_product_category",
    "fit": "kfps_fit",
    "material": "kfps_material",
    "color": "kfps_color",
    "season": "kfps_season",
    "occasion": "kfps_occasion",
    "style_tone": "kfps_style_tone",
    "target_hypothesis": "kfps_target_hypothesis",
    "description": "kfps_concept_description",
}
```

주의: Streamlit은 위젯 렌더 **전에** session_state를 써야 반영된다. 분해 버튼이 필드들보다 아래에 있으면 이미 렌더된 위젯엔 값이 안 들어감 → 주입 직후 `st.rerun()` 한 번 호출이 가장 단순한 해법. (이미지 보조도 같은 이슈가 있음 — rendering.py:1108은 description 위젯보다 위라서 동작하는 구조)

### 4-3. 선택지 칩 전환 (P2)

- `STYLE_TONE_PRESETS` + "직접 입력" 패턴(rendering.py:1168-1181)이 이미 있으니 핏/시즌/착용 상황에 복제
- 옵션 상수는 `app_config.py`에 모으기 (KOREA_PROVINCE_OPTIONS와 같은 위치)
- 멀티 선택이 자연스러운 시즌은 `st.pills` 또는 `st.multiselect`

### 4-4. 이미지 보조 강화 (P7) — image_assist.py

- env 의존 제거: `configured_image_analyzer_from_env()` 앞에 세션 기반 팩토리 `image_analyzer_for_session(provider, api_key, model)` 시도. 호출부는 rendering.py:1087-1092 한 곳
- provider 추가: `openai_image_analyzer` 패턴 복제. Anthropic은 `POST /v1/messages` + `{"type": "image", "source": {"type": "base64", ...}}`, Google은 `generateContent` + `inline_data`. 시그니처를 `ImageAnalyzer`와 동일하게 유지하면 `make_image_concept_draft`는 무수정
- vision 지원 여부는 `config/pricing_config.yaml`에 `supports_vision: true` 플래그 추가로 관리, 미지원 모델 선택 시 토글에 안내 문구

### 4-5. 테스트 포인트

- analyzer/parser를 mock callable로 주입하는 기존 image_assist 테스트 패턴 따라가기
- JSON 파싱 실패 → description fallback 경로 테스트 필수
- 자동 채움 후 평가 payload에 원문·이미지 관련 데이터가 새지 않는지 — 기존 raw-image 차단 테스트가 계속 통과해야 함 (이 프로젝트의 핵심 안전 불변식)

### 4-6. 작업 순서 추천

1. **P4 예시 프리셋** — 1시간 미만, 즉시 효과
2. **P3 필수 3개 노출** — UI 재배치만, 반나절
3. **P1 텍스트 자동 분해** — 핵심 가치, 1~2일 (concept_parser.py 신규 + UI 버튼)
4. **P2 선택지 칩** — 반나절~1일
5. **P5 결과 요약 카드** → **P6 프리뷰 모드** → **P7 이미지 보조 강화** 순

P1~P4까지만 해도 "11개 빈 칸 타이핑"이 "붙여넣기 1번 + 클릭 몇 번"으로 줄어든다. 이미지가 전혀 없어도 성립하는 개선이고, 이미지 보조는 그 위의 보너스 옵션.

---

*본 문서는 v0.8.0 코드 리딩 기반 제안이며, 텍스트 전용 빠른 스크리닝이라는 본래 목적과 기존 안전 경계(자동화는 draft 제안까지, 평가 루프에는 사용자 확인 텍스트만)를 전제로 한다.*
